# 批量改备注（BatchEditContactRemarks）未解决问题

本文件随 `BatchEditContactRemarks.kt` 一起提交，记录该功能**已知但尚未修复**的问题，供 reviewer 与使用者知悉。

## 功能概述

`批量改备注` 位于「批量操作」分类。流程：多选好友 → 配置（源字段 / 匹配正则 / 替换串）→ 后台逐个按正则生成新备注并写库。

- 源字段可选 `昵称` 或 `原备注`。
- 新备注通过 `WeDatabaseApi` 直接 `UPDATE rcontact.conRemark` 落库，不解析任何微信类、不实现 `IResolveDex`。
- 规则（正则 / 替换串 / 源字段）通过 `WePrefs` 持久化，跨次运行保留。

## 已知缺陷：通讯录字母分组不刷新

**现象**：批量改备注后，联系人的备注文本（详情页、搜索）已正确更新，但微信通讯录右侧的字母分组（J / C / Z …）**仍停留在旧字母区**，联系人不会移到新字母下。

**根因**：
- 微信通讯录的字母索引是按 `rcontact.pyInitial`（拼音首字母）排序/分组的，而该列由微信进程自己的联系人缓存（`ContactStorage`）派生。
- 本功能走的是「直接 SQL 更新 `rcontact`」路径，虽然 `setRemark` 已同时写入 `pyInitial`/`quanPin`（见 `computePinyin`），但微信**进程内缓存不会因此重算**，列表分组读的是缓存而非实时查表 —— 因此分组字母不变。
- `computePinyin` 已修正为 Latin-first 写法（首字符为拉丁字母时直接取该字母当分组首字母，完全不依赖微信进程内往往不可用的 ICU `Transliterator`），因此 `pyInitial` 列**写的是对的**（完成框的诊断输出可佐证），问题出在「写了微信也不读」，而非「没写对」。

**当前文件内的诊断手段**：
- `readPyInitial(username)` 在完成阶段把库里真实的 `pyInitial` 读回，打印到「修改完成（含诊断）」对话框（最多 6 条样本：`昵称 | 新备注 → pyInitial=xxx`）。
- 若诊断显示 `pyInitial` 以目标字母（如 `Z`）开头、而通讯录仍停在旧字母区，即可确认是上述「微信不读取直接写入列」的缓存问题，而非本功能写入错误。

## 待办（修复方向）

改用**微信自身的联系人更新接口**（`ContactStorageLogic.toModContactOplog` 这类内部 modContact 流程）去更新备注，而非裸写 SQL。该路径会：

1. 让微信自己重算 `pyInitial`/`quanPin`；
2. 通知通讯录列表刷新，使分组字母立即归位；
3. 全程纯本地（走微信进程内 oplog），**不涉及任何服务器上传**，无云控风险。

该修复需要 DexKit 锚定微信 `ContactStorage` / modContact 相关方法，存在微信版本兼容成本，尚未实现。在修复落地前，本功能「改备注文本」可用、「挪动字母分组」不可靠。

## 使用提示（给使用者）

- 若只需批量改备注文本，本功能可直接用。
- 若在意通讯录字母分组归位，请等待上述修复，或手动在微信内编辑一次该联系人备注触发微信自身刷新。
- 修改完成后务必**强制停止微信**（设置 → 应用 → 微信 → 强制停止）后重开，否则详情页仍可能读旧缓存。
